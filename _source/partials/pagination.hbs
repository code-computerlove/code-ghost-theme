<nav class="c-pagination o-layout">
    <div class="o-layout__left u-1/3">
		{{#if prev}}<a href="{{page_url prev}}">Newer Posts</a>{{else}}&nbsp;{{/if}}
	</div>
    <div class="o-layout__left u-center u-1/3"> Page {{page}} of {{pages}} </div>

    <div class="o-layout__left u-right u-1/3">
		{{#if next}}<a href="{{page_url next}}">Older Posts</a>{{else}}&nbsp;{{/if}}
	</div>
</nav>
